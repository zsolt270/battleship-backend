package battleship.battleship_game.enums;

/**
 *  This enum contains all the potential user roles.
 */
public enum Roles {

    /**
     * Default user role.
     */
    USER,

    /**
     * Admin role, that has more privileges than the default user.
     */
    ADMIN;
}
